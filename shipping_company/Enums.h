#pragma once

enum Type
{

	NORMAL,
	VIP,
	SPECIAL

};

enum TrucksData
{

	SPEED,
	CAPACITY,
	CHECKUP_JUOURNEY,
	CHECKUP_DURATION,
	DATASIZE

	//each truck should have its cap, sp,size
	//diff bet size and cap

};

enum UIModes
{

	INTERACTIVE,
	STEPBYSTEP,
	SILENT

};
